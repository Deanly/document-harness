# Legacy Single-Instance Operations

This historical document assumes one application instance and conflicts with the current availability design. The conflict requires review rather than automatic resolution.
