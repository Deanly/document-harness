# QA

이 파일은 현재 읽어야 하는 `qa` 문서의 얇은 입구입니다.

## Rules

- `Status: current`인 QA 문서만 적습니다.
- 각 항목은 링크, `qa_type`, source documents, `Updated` 날짜를 남깁니다.
- `draft`는 번호 reservation이므로 이 목록에 올리지 않습니다.
- `retired`가 되면 목록에서 제거하고 대체 QA 문서나 종료 근거를 본문에 남깁니다.

## Active

- 현재 current QA 문서가 없습니다.
